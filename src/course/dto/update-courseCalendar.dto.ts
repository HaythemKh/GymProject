import { Exclude } from 'class-transformer';
import {IsNotEmpty,IsEmail,IsOptional,IsNumber,isNumberString,IsDate,IsString,ArrayMinSize,IsPhoneNumber, IsBoolean, IsDateString} from 'class-validator'

export class UpdateCourseCalendarDto{

    // @IsDateString()
    // StartDate : string;

    // @IsDateString()
    // EndDate : string;
}
